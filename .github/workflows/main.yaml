on: [pull_request]

jobs:
  pr_match_job:
    runs-on: ubuntu-latest
    name: A job to validate PR is in a valid project
    steps:
    - name: Check PR is in project
      id: match
      uses: js-egton/themis@v0.3
      with:
        match-regex: ^/Project [\d]{2}/